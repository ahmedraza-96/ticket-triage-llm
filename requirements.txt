# Local CPU-side pipeline (Windows box): data prep, Groq arm, scoring, judging, reporting.
# GPU-side pins live in requirements-gpu.txt (installed inside Kaggle/Colab sessions).
pandas>=2.0
scikit-learn>=1.3
datasets>=2.19
groq>=0.11
openai>=1.40
python-dotenv>=1.0
matplotlib>=3.8
huggingface_hub>=0.24
